import { ShootingValuesPipe } from './shooting-values.pipe';

describe('ShootingValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShootingValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
