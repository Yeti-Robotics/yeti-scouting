import { HeightValuesPipe } from './height-values.pipe';

describe('HeightValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
