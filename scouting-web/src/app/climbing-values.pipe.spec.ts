import { ClimbingValuesPipe } from './climbing-values.pipe';

describe('ClimbingValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ClimbingValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
